package randomwalk;

import java.awt.*;
import mvc.*;
import simstation.*;

public class RandomView extends SimulationView {
    public RandomView(Model model) {
        super(model);
        setBackground(Color.GRAY);
    }
}
